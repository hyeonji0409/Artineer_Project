$(window).scroll(function () {
	var scrollValue = $(document).scrollTop();
    var weAre = $('#WeAre');

	console.log(scrollValue);
    console.log();

        // if(scrollValue > 500) { // 786
        //     $('#WeAre').fadeTo(1000, 0.0);
        // }
        // // if(scrollValue < 499) {
        // //     $('#WeAre').fadeTo(1000, 1.0);
        // // }
        // if(scrollValue > 1400) {
        //     $('#Major').fadeTo(1000, 0.0);
        // }
        // // if(scrollValue < 1399) {
        // //     $('#Major').fadeTo(1000, 1.0);
        // // }
        
});